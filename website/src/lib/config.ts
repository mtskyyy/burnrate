// Single source of truth for outbound links and the current release.
// Bump APP_VERSION on each release so the download links stay valid.
export const APP_VERSION = '0.2.0'
export const SITE_URL = 'https://burnrate.run'
export const GITHUB_URL = 'https://github.com/mtskyyy/burnrate'
export const DOWNLOAD_URL = `${GITHUB_URL}/releases/latest/download/BurnRate_${APP_VERSION}_aarch64.dmg`
